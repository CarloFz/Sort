#include <vector>
using namespace std;
namespace Encore
{
    class SortAlgorithm
    {
    public:
        static void BubbleSort(vector<int>& array);
        static void SelectSort(vector<int>& array);      
    };
}
